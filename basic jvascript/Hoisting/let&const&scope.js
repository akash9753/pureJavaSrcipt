//yes let & const are hoisted 
//but they are hoisted very differently than var
//let & const are not hoisted is incorrect infact these are un the temporal dead jone for the time being

//temporal dead Zone : from hoisting till it assigned or get some value or initilized that face is known as 
//temporal dead jone  
//whenever we try to access a variable inside a temporal dead zone it gives u reference error
